# 引き継ぎ書 — じぶんNaviベース

**このファイルを最初に読んでください。**（人間用の手順書です。Claude Code向けの指示は `CLAUDE.md` にあります）

作成日：2026年8月19日
引き継ぎ元：Claude（チャット）／引き継ぎ先：Claude Code（自宅Windows PC）

---

## 1. このプロジェクトを一言で

**「次に曲がる方向を教えるだけでなく、その先まで考えて、今どの車線を走るのが一番いいのかを教えてくれる自分専用カーナビ」**

最終目標は Android の AIBOX（車載ディスプレイ）で動かすこと。
現在は **Phase 1：日本の道路でMapboxの車線データが本当に使えるのかを測定する段階**。

---

## 2. 今どこまで進んでいるか

| 状態 | 内容 |
|---|---|
| ✅ 完了 | 技術調査（Mapbox・料金・車線データの取得方法） |
| ✅ 完了 | 開発方式の決定（Webアプリ／費用¥0） |
| ✅ 完了 | 仕様書3点の作成 |
| ✅ 完了 | **Phase 1（Step 1〜11）実装**：地図・GPS追従・画面消灯防止/音声解禁・SmartLane本体（自動テスト20件通過）・目的地検索・自動再ルート・走行ログ・集計画面 |
| ✅ 完了 | **Phase 2（初期）**：全画面フローティングUI・設定画面（`settings.html`）・音声検索・目的地マーカー |
| ✅ 完了 | **Phase 2A**：他社カーナビ(COCCHi等)を参考にした実走用ナビパネル・到着判定・標準音声案内・SmartLane専用表示・通信/GPS状態表示（自動テスト追加、`appLogic.test.html`が79件通過） |
| ⬜ 次 | **実車での走行確認**（`docs/05_Phase2A実走確認チェックリスト.md`）。その後、Phase 2B以降（`docs/04_COCCHi比較・今後の改善計画.md`、設計のみ・未実装） |

Phase 1〜2Aのコードは自動テスト（`tests/smartLane.test.html` 20件、`tests/appLogic.test.html` 79件）をすべて通過済みです。**ただし実車での走行確認はまだ行っていません。**

---

## 3. 家のパソコンでやること（この順番で）

### ① Mapboxトークンを作る（10分）

1. https://account.mapbox.com/ にログイン
2. Tokens →「Create a token」
3. Public scopes はデフォルトのままでOK
4. **URL restrictions に次を追加** ← 必須
   ```
   https://<自分のGitHubユーザー名>.github.io/*
   ```
5. `pk.` で始まるトークンをコピー

> URL制限をかけないと、他人にトークンを使われて無料枠を消費される恐れがあります。
> `sk.` で始まる秘密トークンは絶対に使わないでください。

### ② トークンを設定する（1分）

`js/config.js` の1行を書き換えます。

```js
export const MAPBOX_TOKEN = 'pk.ここに自分のトークンを貼る';
```

### ③ GitHubに置いて公開する（10分）

1. GitHubで新規リポジトリを作成
   - 名前：`jibun-navi-base`
   - 公開設定：**Public でOK**（トークンにURL制限があるため安全）
2. GitHub Desktop でクローン → このフォルダの中身をコピー → コミット → プッシュ
3. リポジトリの Settings → Pages → Source を `main` ブランチのルートに設定
4. 数分後、次のURLで開けます
   ```
   https://<ユーザー名>.github.io/jibun-navi-base/
   ```

### ④ iPhoneで地図が出るか確認する（3分）

1. SafariでそのURLを開く
2. 画面右のDEBUGパネルの「状態」を見る
   - **「地図OK（Step 1完了）」** → 成功。Step 2へ進めます
   - 「トークン未設定」 → ②をやり直す
   - 「地図エラー」 → URL制限の設定を確認する
3. 成功したら、共有ボタン →「ホーム画面に追加」

### ⑤ Claude Codeを起動して引き継ぐ

プロジェクトフォルダで Claude Code を起動し、次をそのまま貼り付けてください。

```
CLAUDE.md と docs/ 配下の3つのドキュメントを全部読んでから作業を始めてください。
Step 1 は完了済みで、iPhoneで地図表示まで確認できています。
docs/02_Phase1実装計画.md の Step 2（現在地追従とGPS精度表示）から実装してください。
```

---

## 4. これまでの決定と、その理由

後から「なぜこうしたんだっけ」とならないよう記録します。

| 決定 | 理由 |
|---|---|
| **ネイティブアプリではなくWebアプリ** | WindowsではiPhoneアプリを作れない（Xcodeが Mac専用）。Webなら Windows開発・iPhoneテスト・AIBOX本番がすべて成立する |
| **費用¥0の構成** | GitHub Pages無料、Mapbox無料枠内。Apple Developer（年$99）もMacも不要 |
| **npm・ビルドツールを使わない** | CDN読み込みだけにすることで、pushすれば即iPhoneで確認できる。開発サイクルが最短になる |
| **リポジトリはPublic** | トークンにURL制限をかけるため安全。Privateだと GitHub Pages が有料になる |
| **iPhone先行 → AIBOX** | Androidスマホを持っていないため。Webアプリなので移行は容易 |
| **`js/core/` を依存ゼロにする** | 将来ネイティブAndroid版を作るとき、Kotlinへそのまま写せるようにするため |
| **Phase 1 を測定に徹する** | 日本の車線データが使えるか未知数。使えないと分かった時点で早く方針転換できるようにする |

---

## 5. 絶対に守るルール

### SmartLane 安全原則（8項目）

1. 取得できない車線情報を推測で補完しない
2. `isValid === false` の車線は絶対に推奨しない
3. GPS精度が悪い場合は独自車線案内を抑制する
4. 直前の無理な車線変更を促さない
5. Confidenceが LOW / UNKNOWN の場合は独自車線音声案内を出さない
6. Mapbox標準案内を常にフォールバックとして残す
7. SmartLaneは「最短ルート」ではなく「安全かつ次の操作につながりやすい車線」を選ぶ
8. **データ不足時は、案内しないことを正解とする**

### 課金事故の防止

Mapboxには支出上限の仕組みがありません。次を必ず守ってください。

- Directions API を呼ぶのは「ナビ開始時」と「ルート逸脱時（**最短10秒間隔**）」のみ
- **位置が更新されるたびに呼ぶ実装は絶対禁止**（1秒に1回なら1時間で3,600回）
- 検索は検索ボタンを押した時のみ
- Mapbox管理画面で**使用量アラート**を設定しておく

---

## 6. 残りの作業（Step 2〜11）

| Step | 内容 | 難所 |
|---|---|---|
| 2 | 現在地追従・3D・GPS精度表示 | — |
| **3** | **画面消灯防止と音声の解禁** | **最大の関門。ここが動かないと実走できない** |
| 4 | Core層のデータ型定義 | — |
| 5 | Directions API と正規化 | `roadClass` の取り出し方 |
| 6 | routeTracker（自前のナビ進行管理） | Turf.jsでの位置吸着 |
| 7 | SmartLane本体＋比較モジュール＋テスト20件 | 仕様の中核 |
| 8 | デバッグ表示・車線矢印描画 | — |
| 9 | 目的地検索・自動再ルート | API呼びすぎ注意 |
| 10 | 走行ログ | 記録頻度に注意 |
| 11 | 集計画面（全体／道路種別／ルート／Mapbox比較） | — |

詳細は `docs/02_Phase1実装計画.md` を参照。

---

## 7. つまずきやすいポイント

| 症状 | 原因と対処 |
|---|---|
| 地図が真っ白 | トークン未設定かURL制限のミス。DEBUGパネルの「状態」に理由が出ます |
| 走行中に音声が鳴らない | iOS Safariはユーザー操作起点でないと発話しない。「ナビ開始」ボタン内で空の発話を1回実行して解禁する |
| 走行中に画面が消える | `navigator.wakeLock.request('screen')` を使う。画面復帰時に再取得する処理も必要 |
| 位置情報が取れない | HTTPS必須。GitHub Pagesなら問題なし |
| 電池がすぐ減る | GPS＋画面つけっぱなしのため。**シガーソケット給電が必須** |
| 自車位置が飛ぶ | トンネル・高架下。これ自体は正常。GPS精度による案内停止が効いているか確認する |

---

## 8. 未確定・要確認の事項

実装や購入の前に確認が必要なものです。**推測で進めないでください。**

1. **Wake Lock API（画面消灯防止）がiPhoneで実際に動くか** — Step 3で実機検証。動かない場合は「無音動画のループ再生」で代替
2. **Mapbox GL JS の最新安定版** — 現在は unpkg のメジャーバージョン指定（`@3`）。本番運用時は固定版に切り替える
3. **Mapboxの無料枠の数値** — 変動するため mapbox.com/pricing で再確認
4. **日本の車線データの実際の取得率** — これがPhase 1で測る本題
5. **AIBOXの機種選定** — 購入前に「Androidバージョン」「GPS内蔵の有無」「RAM容量」を確認

---

## 9. Phase 1 が終わったあとの判断

車線データの取得結果によって、3パターンで判断します。

| パターン | 状況 | 次の方針 |
|---|---|---|
| **A** | 全道路種別で十分に取得できる | SmartLaneを全面採用してPhase 2へ |
| **B** | 高速・国道・主要道路は十分、地方道は不足 | 対象道路を限定してSmartLaneを有効化してPhase 2へ |
| **C** | ほぼ取得できない | SmartLane方式そのものを再検討 |

**取得率が低くても即終了ではありません。** 高速・国道で使えるなら十分に価値があるため、`roadClass` ごとに有効／無効を切り替えられる設計にしてあります。

---

## 10. フォルダ構成

```
jibun-navi-base/
├─ HANDOFF.md          ← このファイル（人間用の手順書）
├─ CLAUDE.md           ← Claude Codeへの指示書
├─ README.md           ← セットアップ手順
├─ docs/
│   ├─ 00_調査報告.md          技術調査の結果と制約
│   ├─ 01_SmartLane仕様.md     中核ロジックの仕様（安全原則・テスト20件）
│   ├─ 02_Phase1実装計画.md    Step 1〜11の手順と判定方法
│   ├─ 03_VOICEVOXナビ音声仕様（将来フェーズ）.md
│   ├─ 04_COCCHi比較・今後の改善計画.md   他社比較とPhase2B以降の設計（未実装）
│   └─ 05_Phase2A実走確認チェックリスト.md  同乗者向け実走確認チェックリスト
├─ index.html
├─ settings.html       ← 設定画面
├─ stats.html          ← 走行ログ集計画面
├─ manifest.json
├─ css/style.css
├─ js/
│   ├─ config.js       ★トークンとしきい値。まずここを編集
│   ├─ main.js
│   ├─ core/           依存ゼロ。将来Android移植でそのまま使う
│   ├─ platform/       Mapbox・ブラウザAPI依存
│   ├─ nav/            ナビ進行の自前実装
│   ├─ ui/
│   └─ log/
└─ tests/               smartLane.test.html（20件）／appLogic.test.html（79件）
```

---

## 11. Claude Codeへの伝え方のコツ

- 「Step ◯を実装して」と**ステップ単位で区切って**依頼すると、応答が途中で切れにくくなります
- 応答が切れたら、続きを書かせず **「再開」** とだけ送ってください（ファイルの現在状態を確認してから続ける指示が `CLAUDE.md` に入れてあります）
- 大きな修正は「差分で見せて」と伝えると、やり取りが軽くなります
- 実機で試して不具合が出たら、**DEBUGパネルの表示内容をそのまま伝える**と原因特定が早いです
